import styled from "styled-components";
import { colors } from "./Palette";

export const Label = styled.label`
  color: ${colors.label};
  font-size: 12px;

  display: flex;
  align-items: center;
  gap: 4px;
`;
